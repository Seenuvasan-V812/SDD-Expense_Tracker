spring:
  kafka:
    bootstrap-servers: "localhost:29092"
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

# Disable OutboxRelayScheduler and BudgetRolloverService in non-Kafka tests
scheduling:
  enabled: false
